{
  "name": "makefast-proxy",
  "description": "",
  "version": "1.0.0",
  "dependencies": {
    "chalk": "^2.3.0",
    "express": "^4.16.2",
    "request": "^2.85.0",
    "ws": "^4.0.0"
  },
  "devDependencies": {
    "shelljs": "^0.7.8"
  },
  "scripts": {
    "dev": "node server --debug",
    "local-proxy": "docker-compose -f docker-compose.local-proxy.yml up",
    "build": "docker build . -t docker.baqend.com/makefast/makefast-proxy:latest",
    "publish": "docker push docker.baqend.com/makefast/makefast-proxy:latest"
  }
}
